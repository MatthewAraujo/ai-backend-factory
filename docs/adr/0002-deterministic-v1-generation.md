# Deterministic generation for v1

AI Backend Factory v1 will create Generated Services from local blueprint-aligned templates and assembly rules instead of using an LLM to produce files. We chose this because the first release needs reliable, testable, repeatable output that stays tightly aligned with `forum-blueprint.md`, while AI-assisted domain generation is planned as a later step in the creation flow.
