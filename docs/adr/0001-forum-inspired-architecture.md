# Forum-inspired modular monolith for factory and generated services

AI Backend Factory will use the architecture style from `forum-blueprint.md` for both the Factory Service and every Generated Service. We chose this over using a simpler internal architecture for the factory because the shared shape keeps the generator aligned with its own output, reduces cognitive overhead, and makes the blueprint a direct engineering reference instead of a one-off scaffold source.
